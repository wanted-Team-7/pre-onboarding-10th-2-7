import * as S from '../styles/searchInput.style';

const SearchInput = () => {
  return (
    <S.StyledPlaceholder>
      <S.StyledText>질환명을 입력해 주세요</S.StyledText>
    </S.StyledPlaceholder>
  );
};

export default SearchInput;
